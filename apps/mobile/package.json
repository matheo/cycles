{
  "name": "@cycles/mobile",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "cap:add": "cap add",
    "cap:copy": "cap copy",
    "cap:open": "cap open",
    "cap:sync": "cap sync",
    "cap:update": "cap update",
    "postinstall": "[ -d www ] && cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^2.4.7",
    "@capacitor/core": "^2.4.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^2.4.7"
  }
}
